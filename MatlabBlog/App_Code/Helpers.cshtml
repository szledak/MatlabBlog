@helper Tags(System.Web.Mvc.HtmlHelper htmlHelper, IList<MatlabBlog.Core.Objects.Tag> tags)
{
  foreach (var tag in tags)
  {
    <div class="tags-div">@MatlabBlog.ActionLinkExtensions.TagLink(htmlHelper, tag)</div>     
  }
}